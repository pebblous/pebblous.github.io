---
name: ko-prose-humanizer
description: >
  한국어(및 영어) 글에서 "AI가 쓴 티"가 나는 문체를 진단·점수화하고, 사실·수치·인용·구조는
  그대로 둔 채 산문 질감만 사람 글로 교정한다. 블로그 글·리포트·뉴스레터·보도자료 초안을
  검토할 때, 사용자가 "AI 티가 난다", "문체 다듬어줘", "이거 사람이 쓴 것처럼",
  "평가하고 고쳐줘", "deslop", "de-AI", "AI스러움 지우기"라고 하거나, 갓 생성된 긴 한국어
  산문을 게시 전 검수할 때 반드시 사용한다. 단순 오탈자 교정이 아니라 LLM 특유의 문체 패턴
  (줄표 재진술, 명사형 종결, 상투 비유, 메타 예고문 등 11종 tell)을 잡아내는 작업이면
  이 스킬을 쓴다. 본문뿐 아니라 제목·부제목(mainTitle·subtitle)의 슬롭도 같은 11 tell 렌즈로 교정한다.
argument-hint: "[path-to-html-or-md] [--report-only|--apply] [--threshold=0.3]"
---

# ko-prose-humanizer: 한국어 AI 문체 평가·교정

> 페블러스 산문 검수의 마지막 단계. voice-edit이 "어떤 보이스로 쓸지"를 다룬다면
> 이 스킬은 "AI가 쓴 티"라는 표면 질감을 측정·교정한다.

## 핵심 원칙 — 무엇을 바꾸고 무엇을 지키는가

생성형 모델이 쓴 한국어 산문은 사실·논리가 멀쩡해도 **표면 질감**에서 티가 난다.
이 스킬은 그 질감을 진단하고, 내용은 건드리지 않은 채 문장 결만 사람 글로 되돌린다.

내용이 아니라 질감을 고친다. 다음은 **절대 변경 금지**:

- 숫자·통계·날짜·고유명사·인용문(따옴표 안 원문)
- 인용 출처, 링크, DOI, 참고문헌, 각주
- 프런트매터·메타데이터, 목차, 표의 데이터, 이미지 캡션의 사실
- 글의 논리 구조와 소제목 체계, 단락 순서
- HTML 구조·클래스·속성

바꾸는 것은 오직 **문장의 리듬·종결·연결·어휘 선택**이다. 의심스러우면 보존 쪽으로
판단한다. 사실을 "더 매끄럽게" 한다며 수치를 반올림하거나 표현을 바꾸지 않는다.

---

## 1단계 — 진단 (AI tell 11종)

각 항목을 0~10으로 채점한다(높을수록 AI 티가 강함). 실제 본문에서 증거 문구를 1~2개씩 뽑는다.

### 문장 단위 (T1~T9)

| 코드 | tell | 신호 | 교정 방향 |
|---|---|---|---|
| T1 | **줄표(—) 동격 재진술** | "~이다 — ~다", "옮긴다 —" 처럼 대시 뒤에 같은 말을 다시 풂. 한 글에 수십 회면 위험 | 대시 대부분 제거. 재진술이면 한 문장으로 합치거나 둘 중 하나만 |
| T2 | **명사형 종결 일색** | 문장 끝이 거의 다 "~이다/~다/~점이다/~사실이다". 동사 종결 거의 없음 | 종결을 섞는다. 동사로 끝나는 문장, 의문·짧은 문장 의도적 배치 |
| T3 | **상투 비유·"한 X" 반복** | 좌표·합주·악장·결·층위·화면 같은 비유 남발, "한 줄/한 문장/한 자리/한 좌표" 반복 | 비유는 글 전체에 1~2개만. "한 X"는 대부분 평범한 표현으로 |
| T4 | **메타 예고문** | "옮긴다", "풀어 쓰자면", "짚어 본다", "묶어 본다", "한 발 더 들어가면", "다시 읽어 본다" | 예고 없이 바로 내용으로. 다음에 뭘 할지 굳이 알리지 않음 |
| T5 | **영어 병기 강박** | 거의 모든 개념에 (English) 괄호 병기 | 첫 등장 핵심어만 병기, 나머지 삭제. 한국어 정의·이름 우선, 영어 괄호 보충 |
| T6 | **규칙적 볼드 요약** | 모든 섹션이 볼드 한 줄 요약으로 끝나는 동일 패턴 | 요약 자체는 두되 형식을 깨뜨림. 일부는 볼드 없이, 일부는 통합 |
| T7 | **번역투** | 불필요 수동태, 과한 명사화, "~에 있어서", "~을 부른다", "~을 가능하게 한다", "~라는 진단이다" | 능동·구어체 한국어로. 주어-동사 직결 |
| T8 | **수치·고유명사 과적재** | 한 문장에 F1·κ·배수·연도를 몰아넣어 SEO 키워드처럼 보임 | 핵심 수치만 본문에, 나머지는 분산하거나 괄호·각주로 |
| T9 | **병렬·3단 강박** | "A·B·C", "도덕→학술→법" 식 대구를 모든 문단에서 반복 | 일부는 풀어서 서술. 병렬은 정말 효과적인 곳만 |

### 구조 단위 (T10~T11)

문장을 다 고쳐도 AI 티가 남는다면 골격 문제다. 다음 둘은 단락·글 단위로 본다.

| 코드 | tell | 신호 | 교정 방향 |
|---|---|---|---|
| T10 | **정보 구조 — 덤프 vs 흐름** | 도입부·요약이 결론 대신 수치·고유명사를 한 단락에 쏟아붓는다. "그래서 결론이 뭔데"가 한참 뒤에 나옴. C레벨이 30초에 핵심을 못 잡음 | 결론 먼저(BLUF). 수치는 가장 센 2~3개만 앞에, 나머지는 본문으로. 단락마다 "독자가 여기서 무엇을 얻는가"를 점검 |
| T11 | **자사·결론 연결의 작위성** | 객관 분석 끝에 "그러므로 우리 제품"으로 점프. 외부 권위(논문·인물)를 자사 정당화로 끌어옴. 분석의 신뢰를 홍보가 깎음 | 자사를 분석의 **결론**이 아니라 분석에 대한 **반응**으로 위치. 본문은 분석으로 끝내고, 홍보는 절제된 editor's note로 분리. "정당화한다"가 아니라 "이런 문제에 관심을 둔다" 톤 |

### 영어 산문 추가 점검

영어 글이면 추가로 확인:
- delve / moreover / furthermore / crucial / paradigm shift / underscore
- em-dash 남용 (영어는 한국어보다 dash 빈도가 더 낮은 게 자연스러움)
- rule-of-three 강박 ("not just X, it's Y", "what X means for Y, Z, and beyond")
- "stands as a testament", "in the realm of", "a tapestry of" 같은 상투구

### 점수 해석

- 항목 합계 / 110 (T1~T11). 0.3 미만이면 사람 글에 가까움, 0.5 이상이면 교정 강권.
- T10·T11은 점수가 낮아도 치명적이다. 문장이 다 깔끔해도 구조가 덤프거나 자사 연결이
  작위적이면 결국 AI 티가 난다. 반드시 별도로 확인한다.
- 점수보다 **증거 문구**가 중요하다. 사용자가 "왜?"를 바로 확인할 수 있어야 한다.

#### 0.3~0.5 "온건 교정" 구간 — 5편 누적 학습 (2026-06-01 추가)

페블러스 시리즈 5편 모두 **0.32~0.45 구간**에 있었다. 즉 자동 교정 강권 임계치(0.5)는
넘지 않았지만 통과 임계치(0.3)도 넘어선, **수동 검토 권장 구간**.

이 구간의 글은 voice-edit default를 따라 처음부터 작성된 글이 많다. 거시 7원칙(위키체·광고체·
AI 접속부사 0)은 통과하지만 미시 11 tell(em-dash·메타 예고문·자사 점프)에서 잔여 신호가 남는다.

**0.3~0.5 구간의 실무 처리**:
1. 진단만 출력하고 자동 교정은 보류
2. T1 em-dash 빈도가 1/300자 미만이면 그 자체로 자동 교정 트리거 (점수 무관)
3. T11 자사 점프가 본문 마지막에 있으면 Editor's Note 분리 자동 권고
4. 나머지는 사용자 검토 위임

이 4단계 처리로 5편이 모두 약 50%씩 em-dash가 감소했다.

---

## 2단계 — 자동 검출 grep 패턴 (페블러스 캘리브레이션)

진단을 빠르게 시작하기 위해 grep으로 1차 신호를 잡는다. JH 보이스 정본(`code-painting-essay`)과
AI 출력의 차이로 보정된 임계치.

### 한국어 (대상 파일에 실행)

```bash
FILE="<대상 파일 경로>"

# T1 em-dash 빈도 (본문에서 1/500자 이하 권장. 1/300자 이하는 위험)
python3 << 'EOF'
import re
t = open("$FILE").read()
t = re.sub(r'<meta[^>]*>', '', t)
t = re.sub(r'<script[^>]*>.*?</script>', '', t, flags=re.DOTALL)
text = re.sub(r'<[^>]+>', '', t)
text = re.sub(r'\s+', ' ', text)
n = text.count('—'); c = len(text)
print(f"본문 글자: {c}, em-dash: {n}, 빈도: 1/{c//max(n,1)}자")
EOF

# T2 명사형 종결 (위험 신호어 검사)
grep -cE "이다\.|점이다\.|사실이다\.|것이다\." "$FILE"

# T3 "한 X" 반복
for kw in "한 줄" "한 문장" "한 자리" "한 좌표" "한 가지" "한 화면"; do
  c=$(grep -c "$kw" "$FILE"); [ "$c" -gt 3 ] && echo "  $kw: $c (반복 위험)"
done

# T4 메타 예고문
for kw in "옮긴다" "풀어 쓰자면" "짚어 본다" "묶어 본다" "한 발 더 들어가면" "다시 읽어 본다" "다음 표가"; do
  c=$(grep -c "$kw" "$FILE"); [ "$c" -gt 0 ] && echo "  $kw: $c"
done

# T7 번역투
for kw in "에 있어서" "을 부른다" "라는 진단이다" "을 가능하게 한다" "되어진" "된 것이다"; do
  c=$(grep -c "$kw" "$FILE"); [ "$c" -gt 0 ] && echo "  $kw: $c"
done

# 기존 검증과 동시 실행 — voice-edit 공통 항목
echo "위키체:"; grep -cE "본 보고서|규명한다|분석하고자|살펴본다|고찰한다" "$FILE"
echo "광고체:"; grep -cE "혁신적|획기적|놀라운|차세대|게임 체인저" "$FILE"
echo "AI 접속부사:"; grep -cE "흥미롭게도|주목할 만한|결론적으로|이러한 맥락에서" "$FILE"
```

### 영어 (EN 글)

```bash
# delve, moreover 등 상투구
for kw in "delve" "moreover" "furthermore" "crucial" "paradigm shift" "underscore" "leverage" "stands as a testament" "in the realm of" "It is worth noting"; do
  c=$(grep -ci "$kw" "$FILE"); [ "$c" -gt 0 ] && echo "  $kw: $c"
done
```

### 정본 캘리브레이션 비교 (반드시 함께 실행)

```bash
# JH 보이스 정본 — 비교 기준선
python3 << 'EOF'
import re
for path in [
  "project/DAL/code-painting-essay/ko/index.html",  # JH 직접 글
  "<대상 파일>",                                       # 검사 대상
]:
    t = open(path).read()
    t = re.sub(r'<meta[^>]*>|<script[^>]*>.*?</script>', '', t, flags=re.DOTALL)
    text = re.sub(r'<[^>]+>', '', t)
    text = re.sub(r'\s+', ' ', text)
    n = text.count('—'); c = len(text)
    print(f"{path}: chars={c}, dashes={n}, 1/{c//max(n,1) if n else 0}자")
EOF
```

---

## 3단계 — 교정

위 11종을 순서대로 적용하되, 한 문장을 고칠 때 여러 tell이 겹치면 한 번에 처리한다.

교정 후 자가 점검:
- 소리 내어 읽었을 때 문장 길이와 종결이 다양한가?
- 대시·"한 X"·메타 예고문이 눈에 띄게 줄었는가?
- 삭제·변경한 곳에서 사실/수치/인용/링크가 그대로 보존됐는가?
- 글쓴이의 원래 관점·강조점이 유지됐는가? (저자 목소리를 내 목소리로 덮지 않는다)

**과교정 금지.** 모든 대시를 0으로, 모든 병렬을 해체할 필요는 없다. 사람도 쓴다.
목표는 "규칙적 반복"을 깨는 것이지 기법 자체를 박멸하는 게 아니다.

---

## 4단계 — 출력 포맷

ALWAYS 다음 순서로 출력한다:

```markdown
## 진단 요약

| Tell | 점수 (0-10) | 대표 증거 |
|------|------------|----------|
| T1 em-dash 재진술 | 7 | "본 글의 한 줄 요약은 — 개인은 강해진다, 과학은 좁아진다." |
| T2 명사형 종결 | 6 | ... |
| ... | ... | ... |

총점: 47/110 (0.43) — 교정 권장.

가장 두드러진 tell 3개:
- T1 (em-dash 117개, 1/237자) → 가장 시급
- T4 메타 예고문 (12개) → 빈번
- T11 자사 연결 → Editor's Note 분리 권장

## 압축 전후표 (고신호 10~15개)

| # | tell | 전 | 후 |
|---|------|-----|-----|
| 1 | T1+T7 | 외로운 군중이라는 표현은 즉시적 직관을 부른다. | '외로운 군중'은 직관적으로 와닿는 말이다. |
| 2 | T4 | 결과는 abstract에 압축되어 있다. 옮긴다. | 결과는 초록 한 단락에 압축돼 있다. 그대로 옮긴다. |
| ... | ... | ... | ... |

> 전체를 표로 만들지 말 것. 패턴을 보여주는 대표 사례만.

## 교정 원문

- 게시 가능한 완성본 전체. 긴 글이면 파일로 저장하고 경로를 제시.
- 프런트매터·표·참고문헌·링크는 그대로 옮기고 산문만 교정.
```

긴 글(대략 본문 1,500자 초과)은 교정 원문을 `.md` 파일로 저장한다:
`_workspace/[글-slug]/voice_humanized.md`

짧은 글이면 인라인으로 출력한다.

---

## 적용 예시 — 페블러스 5편 누적 캘리브레이션 (2026-05-31~06-01)

ko-prose-humanizer 도입 직후 AI 거버넌스 시리즈 5편(KO 4 + EN 1)을 사후 검수한 결과.
**평균 em-dash 46% 감소, 위키체·메타 예고문·자사 점프 모두 0건 수렴.**

### 5편 누적 표

| 글 | 발행일 | em-dash Before | After | 빈도 변화 | 총점 추정 |
|---|---|---|---|---|---|
| Evans Nature 2026 KO | 2026-05-31 | 117 | 52 | 1/237 → 1/494 | 0.45 → 0.18 |
| Evans Nature 2026 EN | 2026-05-31 | 143 | 70 | 1/320 → 1/621 | 0.35 → 0.15 |
| SkillOpt KO | 2026-05-27 | 73 | 49 | 1/277 → 1/407 | 0.32 → 0.20 |
| MUSE-Autoskill KO | 2026-05-28 | 98 | 51 | 1/238 → 1/451 | 0.36 → 0.18 |
| **PIPEDA KO** | 2026-05-29 | 114 | 69 | 1/238 → 1/389 | 0.38 → 0.22 |

**누적**: em-dash 545 → 291 (-46.6%). 5편 모두 0.3~0.5 구간의 **온건 교정** 케이스였고, 자동 교정 강권(0.5+) 임계치엔 도달하지 않았음. 시리즈가 처음부터 voice-edit default를 따라 작성되었기 때문.

### Tell별 5편 공통 패턴

#### T1 em-dash 동격 재진술 — 가장 빈번

**Before (Evans/SkillOpt/MUSE/PIPEDA 공통):**
> 같은 단락의 결론을 옮긴다 — "..."
> 한 줄로 옮기면 이렇다 — 학습이란 외부 자연어 상태에 대한 최적화다.
> 결정문은 다섯 개의 위반을 확정했다 — 개인정보 과다 수집, 동의 결여, ...

**After:**
> 같은 단락은 이렇게 끝난다. "..."
> 풀어 쓰면 학습이란 외부 자연어 상태에 대한 최적화라는 선언이다.
> 결정문은 다섯 개의 위반을 확정했다. 개인정보 과다 수집, 동의 결여, ...

#### T4 메타 예고문 — 글마다 5~10곳, 도입 후 모두 0

| 원문 | 정제 |
|------|------|
| "한 줄로 옮긴다 —" | "Evans는 이렇게 말한다." |
| "옮겨 적어 본다" | 인용 바로 시작 |
| "그 안을 들여다 보자" | 본문으로 직진 |
| "함의를 셋으로 풀어 보자" | "함의는 세 가지로 나타난다" |
| "다시 읽어 본다 —" | "결정문의 한 줄. ..." |
| "한 발 더 들어가면" | 그냥 문단 시작 |

#### T7 위키체 "본 보고서" — MUSE 8, PIPEDA 10 → 모두 0

```
sed 's/본 보고서/이 글/g'
```
단, references·자기참조 메타 (`§N 참조`) 위치는 보존.

#### T11 자사 점프 — Exec 마지막 단락의 카피 톤

**Before 패턴**:
- "페블러스는 이 공백을 정조준한다 — ..."
- "이번 글은 페블러스를 ~ 카테고리 리더로 격상한다"
- "AI 거버넌스 5부작의 마지막"
- "한국어 카테고리로 처음 선언하는 자산이 된다"
- "광고의 형태가 아니라 통찰의 형태로 그 자리를 적어 둔다"

**After 패턴** (Exec에서 제거 → Editor's Note 분리):
```html
<blockquote class="themeable-card rounded-xl p-5 my-6 themeable-text leading-relaxed">
    <p><strong><em>편집자의 노트.</em></strong> [페블러스 동기 + 시리즈 흐름 + 다음 작업 좌표]</p>
</blockquote>
```

5편 모두 Editor's Note에 다음 3요소를 담음:
1. **동기** ("데이터와 AI의 편향을 측정하는 일이라면 페블러스는 늘 구미가 당긴다")
2. **시리즈 흐름** (4부작 또는 시리즈 인라인 링크)
3. **이 글의 자리** ("다섯 번째 글" / "마지막 좌표" / "한국어로 정리하려는 시도")

#### Editor's Note 위치 — 한 곳만 있을 필요 없음 (2026-06-07)

Editor's Note는 **두 위치에 들어갈 수 있고**, 한 곳에만 있어야 하는 게 아니다. 역할이 다르므로 둘 다 둬도 중복이 아니다:

| 위치 | 역할 | 담는 것 |
|------|------|--------|
| **Executive Summary 직후** (표준) | "페블러스가 이 주제를 **왜** 다루나" — 동기·맥락. C레벨이 30초 안에 잡음 | 동기 + (선행글 있으면) 후속 맥락·hub-link |
| **본문 끝 / 마지막 섹션** (선택) | 본문 분석을 다 읽은 뒤 "그래서 페블러스 **시각**은" — 분석에 대한 반응 | 분석 반응 + 다음 작업 좌표 |

- **기본은 Exec 직후 1개**. T11(자사 점프)을 Exec 카피에서 제거할 때, 그 내용을 Exec 직후 Editor's Note로 옮긴다 (stat-card 뒤, section-1 앞).
- 본문이 길고 자사 관점을 본문 분석 뒤에 한 번 더 짚을 가치가 있으면 **마지막 섹션에 두 번째 Editor's Note**를 둔다 (예: turbovec-2026 — Exec=동기, §6=AI-Ready Data 관점 반응).
- ⛔ 두 Note의 내용이 겹치면 안 됨: Exec=동기/왜, 본문끝=반응/시각. 같은 말 반복 금지.
- 실측(2026-06-07): 발행글 8편 중 5편이 Exec 직후, 3편이 본문 끝. **Exec 직후가 주류이자 표준** — 새 글은 최소 Exec 직후 1개를 둔다.

### Stat Cards 정책 (5편 누적)

| 글 | Stat Cards 수 | 정책 준수 |
|---|---|---|
| Evans KO/EN | 4개 | ✅ |
| MUSE KO | 4개 | ✅ |
| PIPEDA KO | 4개 | ✅ |
| SkillOpt KO | **5개 → 4개** | ⚠️ 정책 위반 → 사후 검수에서 정정 |

⛔ **5개 위반 패턴 (SkillOpt 케이스)**: 한 카드를 만들고 싶어질 때마다 본문에 넣을 수 있는지부터 확인. SkillOpt의 "2026-05-22 한미 동시일" 카드는 §6 본문에 풀어 넣음.

### 도입문 패턴 — Stat Cards 위

**Before (5편 공통)**:
> "한 논문이 그은 좌표를 정량으로 풀어 두면 역설의 결이 또렷해진다. ..."
> "MUSE-Autoskill 프레임의 핵심 좌표와 주변 정량 신호를 한눈에 본다. ..."
> "한 결정문이 그은 좌표를 정량으로 풀어 두면 글로벌 규제 도미노의 결이 또렷해진다. ..."

**After (5편 통일)**:
```html
<h3 class="text-xl font-semibold themeable-heading mt-8 mb-3">주요 수치</h3>
<p class="themeable-text-secondary text-sm mb-3">출처: ...</p>
```
도입문 자체를 삭제하고 h3 "주요 수치"로 단순화. **"한눈에 본다·또렷해진다·풀어 두면" 패턴은 T4 메타 예고문의 변종.**

---

## 제목·부제목·SEO제목 — 같은 11 tell 렌즈로 (mainTitle·subtitle·pageTitle)

### 제1 기준 — 제목은 글이 무엇에 관한지 정확·평이하게 알려준다

좋은 제목의 기준은 하나다: **이 글이 무엇에 관한 글인지, 정확하고 쉽게 알려줄 것.** 제목은 멋부리는
자리가 아니라 *알려주는* 자리다. 통찰을 앞세우거나 판단을 연기시키려 들지 말고, 글의 내용을 정직하게
가리키면 된다. 흥미는 주제 자체가 세면 따라온다. 그게 안 나오면 제목이 아니라 *글이* 자기 요점을 아직
못 찾은 것이니, 제목을 꾸미지 말고 글의 요점을 또렷이 해야 한다.

판별은 단 한 번의 동작이다 — **소리 내 읽어, 사람이 실제로 그렇게 말하나.**

| 판정 | 예 | 왜 |
|------|-----|-----|
| ✅ 좋음 | "스스로 연구하고 논문쓰는 AI" | 친구한테 말하듯 *그 글이 무엇인지* 군더더기 없이 알려준다. 구체적이고 꾸밈이 없다 |
| ❌ 나쁨 | "글이 내린 판단을 조용히, 그러나 분명히 말한다" | 글쟁이체 균형 대구라 입으로는 아무도 안 한다. 게다가 **글이 뭐에 관한지 하나도 안 알려준다**(내용 0) |
| ❌ 나쁨 | "새로운 시대" · "온톨로지의 재발명" | 추상·모호. 제목만 보고 무슨 글인지 안 보인다 |

- **묘사여도 좋다.** "라벨은 건조하다"는 오해다. 건조한 건 묘사가 아니라 *추상*이고, 구체적인 묘사는
  그 자체로 생생하다.
- **구체 ≠ 자극.** 우리는 찌라시(충격·미끼)를 쓰지 않는다. 목표는 *정확함*이지 놀라움이 아니다.
  의외의 사실을 *앞세우는* 것과 정직하게 *알려주는* 것은 다르다 — 후자만 한다.

> **슬롭은 본문에만 끼지 않는다. 제목이 가장 먼저 AI 티를 낸다.** 과거 `title-strategy.md`가
> 은유·대조·짧은 선언을 고CTR 패턴으로 *권장*하자, 발굴~리서치~작성 전 단계가 그 지시를 충실히
> 따라 같은 슬롭을 양산했다(2026-06 교훈: "슬롭은 룰브릭이 만든다"). 그 패턴은 본문에서 감점하는
> **T1(줄표 동격)·T2(명사형 미완결)·T7(번역투 반전)** 그 자체다. → **제목·부제목도 본문과 동일한
> 휴머나이저 렌즈를 통과해야 한다.**
>
> **역할 분담(중복 정의 금지)**: 슬롯 정의·글자수·키워드 배치·CTR 패턴 등 **전체 제목 전략의 정본은
> [`docs/title-strategy.md`](../../docs/title-strategy.md)**. 이 스킬은 그 위에서 **"제목의 AI 티"를
> 측정·교정하는 휴머나이저 관점**만 더한다. 슬롯 규칙은 그 문서를 따르고, 여기서 재정의하지 않는다.

### 3슬롯 — 역할이 다르면 렌즈 강도도 다르다

| 슬롯 | 필드 | 휴머나이저 적용 강도 |
|------|------|----------------------|
| **본문 제목** | `mainTitle` (h1) | **가장 엄격.** T1 줄표·콜론 동격 0, T2 명사형 미완결 여운 0, T7 인용+반전 0. **사실 평서문** |
| **부제목** | `subtitle` | 맥락·보조키워드 담당. 슬롯 특성상 줄표 1개까지 허용하되, **mainTitle을 다시 풀어쓰는 동격 재진술 금지** |
| **SEO 제목** | `pageTitle` | 검색 가독성 위해 대시설명형 패턴 허용. **clickbait·과장 어휘(충격·비밀·N가지)만** 차단 |

### ⛔ 제목 슬롭 템플릿 (= 제목에서 발현된 T1·T2·T7·T9)

| 금지 패턴 | tell | 나쁜 예 |
|----------|------|--------|
| 인용 + 반전 | T7 | "'깨끗한 데이터'라고 말했다, 증명은 없었다" |
| 대조 공식 "A가 아니라 B" | T7·T9 | "코드는 열렸다, 데이터는 아니다" · "같은 진단, 다른 미래" |
| **균형 부사쌍·글쟁이체 대구** | T9 | "조용히, 그러나 분명히" · "느리지만 확실히" — *입으로는 아무도 안 하는 말* |
| 줄표/콜론 동격 재진술 | T1 | "온톨로지의 재발명 — 온톨로지를 다시 만들다" |
| 명사형 미완결 여운 | T2 | "~이 사라진 이후" · "~생존 전략" · "~의 조건/과제" |
| clickbait 미끼 | — | "그들이 숨긴 한 가지" · "충격" · "N가지 비밀" |
| 과한 은유 단독 | T3 | "사냥꾼의 곳간, 농부의 땅" (무슨 글인지 안 보임) |

### ✅ 권장 — 사실을 평서문으로 (mainTitle)

장치(은유·반전·대조)는 사실이 약할 때 덧씌우는 분장이고 그게 곧 AI 티다. **사실을 평이하게 말한다.**

| 패턴 | 예시 |
|------|------|
| 주어+동사 사실형 | "챗GPT 이전 유니콘들이 밸류의 68%를 잃었다" |
| 발견 그대로 | "AI 이익의 74%를 상위 20% 기업이 가져갔다" |
| 구체 행위 서술 | "AI가 환자마다 다른 알츠하이머 진행 지도를 그린다" |
| 의외의 사실 | "AI가 기상청을 이겼다" |

> 은유·수치는 **사실을 더 또렷하게 할 때만** 쓴다(장식이 아니라 정보로). "데이터도 유통기한이 있다"처럼
> 한 번에 이해되고 본문 주장과 직결되면 OK.

### 제목 검수 절차 (본문 교정과 함께 1회)

1. `mainTitle`을 위 금지 템플릿에 대조 — 줄표·콜론 동격(T1)·"A가 아니라 B"(T7)·명사형 여운(T2)이 있으면 **평서문으로 교정**.
2. 제목만 읽고 **무슨 글인지** 구체적으로 짐작되는가? 은유만 있고 내용이 안 보이면 탈락.
3. `subtitle`은 보조키워드 1~2개를 유지하되 mainTitle의 말을 다시 풀어쓰지 않는다(동격 재진술 금지).
4. `pageTitle`(SEO)은 슬롯 규칙(`title-strategy.md`)을 따르되 clickbait·과장 어휘만 거른다.
5. **출력**: 4단계 압축 전후표 위에 `제목` 행을 추가해 **mainTitle/subtitle 전·후를 함께** 보인다.

```markdown
| # | tell | 전 | 후 |
|---|------|-----|-----|
| T1 | mainTitle | "온톨로지의 재발명 — 다시 만들다" | "팔란티어가 25년 된 온톨로지를 산업 AI 무기로 바꿨다" |
```

> 캘리브레이션: JH 보이스 정본 `project/DAL/code-painting-essay` — **제목·본문 모두 줄표 0, 평이·구체.**

---

## 다른 스킬과의 관계

| 스킬 | 역할 | 호출 시점 |
|------|------|----------|
| **voice-edit** | 어떤 보이스(default·reflective·sns-cover)로 쓸지 결정 | 글 작성 단계 또는 보이스 변경 시 |
| **ko-prose-humanizer** | 보이스가 정해진 글의 표면 질감 측정·교정 **(본문 + 제목·부제목)** | 게시 전 마지막 검수 단계 |
| **text-reinforce** | 본문 분량·깊이 보강 (사실 추가) | 초고 작성 후 |
| **blog-polish** / **blog-polish-ko** | 제목·리드·소제목 다듬기 (SEO·슬롯 재작성) | 마무리 단계 |
| **sns-write** | 블로그 글의 SNS 카피 생성 | 본문 발행 후. 7-B 단계에서 ko-prose-humanizer 필수 호출 |

> **제목 분업**: `title-strategy.md`(전략 정본) → `blog-polish`(슬롯·SEO 재작성) → **ko-prose-humanizer**(제목의 AI 티 최종 제거). 셋은 같은 anti-slop 기준(T1·T2·T7)을 공유한다.

**호출 순서 권장**: 작성 → text-reinforce → voice-edit (필요 시) → **ko-prose-humanizer** → blog-polish → seo-check → (게시 후) sns-write → **ko-prose-humanizer** (SNS 슬롯별)

### SNS 임계치 — 본문보다 엄격하게 (2026-05-31 추가)

SNS 카피는 본문보다 짧고 호흡이 빠르므로 같은 tell이 더 두드러진다. `sns-write` 7-B 단계에서 ko-prose-humanizer를 호출할 때 다음 임계치를 사용:

| Tell | 본문 임계치 | SNS 임계치 |
|------|-----------|-----------|
| T1 em-dash | 1/500자 이상 | **1/600자 이상** (LinkedIn/Facebook) / **0개** (Twitter/X) |
| T8 수치 과적재 | 한 문장 4개 | **한 슬롯 2개** (Twitter), **한 단락 3개** (LinkedIn) |
| T9 3단 병렬 | 모든 문단 | **슬롯당 1회만** |
| T11 자사 점프 | 점프 금지 | **카피 톤 단언("정조준한다·격상한다·5부작의 마지막") 0건** |
| T4 메타 예고문 | 0 권장 | **0 필수** (한 번이라도 나오면 카피 전체가 가벼워짐) |

SNS 자기검증 자세한 룰은 `.claude/skills/sns-write/SKILL.md` 8절 "SNS 전용 추가 룰" 참조.

---

## 5단계 — 파이프라인 자동 연결

`blog-produce`, `report-produce`, `report-produce-express`의 자기검증 단계에서 자동 호출.

### blog-produce / report-produce Phase 5 (자기검증)

기존 voice-edit 자기검증 grep에 추가로 ko-prose-humanizer 1단계 진단을 실행한다.
총점이 0.5 이상이면 자동 2단계 교정 후 재검증. 0.3~0.5 사이면 보고만 하고 JH 검토에 위임.

### express 모드 분기

`report-produce-express`의 Phase 5-A 자기검토 직후, ko-prose-humanizer 진단을 항상 호출.
JH 검토가 없는 무인 모드이므로 임계치 0.4 이상이면 자동 교정 실행.

---

## 적용 사례 (블로그·리포트 마이그레이션)

### 페블러스 시리즈 추가 검수 대상

ko-prose-humanizer 도입 전 발행된 글 중 em-dash 빈도 1/300자 미만이거나 T11 작위성이
의심되는 글:

- `report/microsoft-skillopt-self-evolving-agents/ko/` — em-dash 1/277자
- `report/muse-autoskill-self-evolving-skill-lifecycle/ko/` — em-dash 1/238자
- `report/openai-pipeda-ai-training-data-regulation/ko/` — em-dash 1/238자

순차 진단·교정 후 한 글당 한 PR.

### BlogScope 평가 에이전트 연동

BlogScope가 글의 내용을 평가할 때 이 스킬의 11 tell 기준을 동시 참조한다. 자세한 사양은
`docs/ai-tone-detection.md` 참조.

---

## 변천 메모

- 2026-05-31 v1: ko-prose-humanizer 초기 등록 (T1~T11 11종 tell + grep 자동화).
  Evans Nature 2026 글의 사후 교정 작업에서 도출된 패턴을 정본화. JH 피드백
  "em-dash 좋게 생각 안 함" + "한국 신문체 헤드라인 톤" + "AI스러움 지우기"가 도입 동기.
- **2026-06-01 v2**: AI 거버넌스 시리즈 5편(Evans KO/EN, SkillOpt KO, MUSE KO, PIPEDA KO)
  사후 검수 완료 후 누적 캘리브레이션 반영. 평균 em-dash 46% 감소(545→291),
  Editor's Note 분리 패턴 + 주요 수치 h3 패턴 정착. 0.3~0.5 "온건 교정" 구간이
  voice-edit default를 따른 글의 전형임을 학습. 4단계 처리 규칙 명문화
  (진단 보류 / em-dash 점수 무관 트리거 / T11 자사 점프 Editor's Note 분리 / 나머지 위임).
- **2026-06-24 v3**: 제목·부제목 교정 섹션 추가("제목·부제목·SEO제목 — 같은 11 tell 렌즈로").
  `title-strategy.md`의 anti-slop 전환(2026-06, "슬롭은 룰브릭이 만든다")을 휴머나이저 안에
  명문화 — 제목 슬롭 템플릿(인용+반전·대조공식·줄표 동격·명사형 여운)을 T1·T2·T7으로 매핑하고
  사실 평서문 권장. 슬롯·SEO 전략은 `title-strategy.md`(정본) 위임, 본 스킬은 제목의 AI 티
  측정·교정만 담당(역할 분담 명시). 도입 동기=AI Scientist v2 글 점검 중 "제목 선택 스킬"
  소재가 휴머나이저에 누락돼 있음을 확인.
